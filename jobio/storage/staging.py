required_staging_fields = {
    "endpoint_url": str,
    "session_vars": str,
    "input_path": str,
    "output_path": str,
}

required_staging_values = {
    "endpoint_url": True,
    "session_vars": True,
    "input_path": False,
    "output_path": True,
}
