required_staging_fields = {
    "enable": bool,
    "endpoint_url": str,
    "session_vars": str,
    "input_path": str,
    "output_path": str,
}

required_staging_values = {
    "enable": True,
    "endpoint_url": True,
    "session_vars": False,
    "input_path": False,
    "output_path": True,
}
